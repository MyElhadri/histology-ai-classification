"""Abstract base model class.

This module defines:
- BaseModel: abstract interface that all model architectures must implement
- Common methods: build(), compile(), summary(), save(), load()
- Ensures consistent API across DenseNet121, ResNet50V2, and Ensemble
"""
