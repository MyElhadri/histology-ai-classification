# Models — Exports

Exported models for deployment and integration.

## Formats

| Format | Use Case |
|---|---|
| TFLite (`.tflite`) | Mobile application deployment |
| ONNX (`.onnx`) | Cross-framework interoperability |
| TensorFlow.js | Web-based deployment |

> These are generated from saved models and are **gitignored**.
